export enum BudgetType {
  FIXED = 'Fixed',
  HOURLY = 'Hourly',
}

export enum ProjectStatus {
  PENDING_REVIEW = 'Pending review',
  OPEN = 'Open',
  IN_PROGRESS = 'In progress',
  COMPLETED = 'Completed',
  CANCELLED = 'Cancelled',
  REJECTED = 'Rejected',
}
